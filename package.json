{
  "name": "engr302-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start-client": "cd client && yarn run dev",
    "start-server": "cd server && mvn spring-boot:run",
    "start-all": "concurrently \"yarn run start-client\" \"yarn run start-server\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://gitlab.ecs.vuw.ac.nz/manninalex/engr302-project.git"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
